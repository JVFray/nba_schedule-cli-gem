require "spec_helper"

describe NbaSchedule do
  it "has a version number" do
    expect(NbaSchedule::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
